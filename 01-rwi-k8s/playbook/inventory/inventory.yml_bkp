---
home:
  hosts:
    localhost:
      ansible_host: localhost
  vars:
    ansible_user: my_user
aws:
  hosts: ~
  vars:
    ansible_ssh_private_key_file: /certs/key_pair.pem
ec2hosts:
  hosts:
  vars:
    ansible_ssh_private_key_file: /certs/key_pair.pem
k8scp:
  hosts:
  vars:
    ansible_ssh_private_key_file: /certs/key_pair.pem
k8s:
  hosts:
  vars:
    ansible_ssh_private_key_file: /certs/key_pair.pem
k8swn:
  hosts: 
  vars:
    ansible_ssh_private_key_file: /certs/key_pair.pem
...
